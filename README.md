# Auto-Language-Switch (ALS)

**Context-aware Input Method Editor (IME) switcher for developers.**

**让中文开发者在写代码时，彻底忘掉切换输入法这件事。**

## 🚀 功能特性 (Features)

*   **智能自动切换**:
    *   **Coding**: 自动切换到 **英文输入法** (English Layout)。
    *   **Commenting**: 自动切换到 **中文输入法** (Chinese Layout, 支持搜狗/微软拼音等)。
*   **一键配置向导 (Setup Wizard)**:
    *   内置 `ALS: Setup Wizard` 命令，自动扫描系统输入法，可视化选择，免去手动查找 ID 的痛苦。
*   **高性能 Sidecar**:
    *   采用 Rust 编写的本地进程，直接调用 Windows 底层 API，响应迅速且稳定。

## ⚙️ 配置 (Configuration)

### 首次使用 (强烈推荐)

插件安装后，请务必运行配置向导来绑定您的输入法：

1.  按 `Ctrl+Shift+P` 打开命令面板。
2.  输入并选择 **`ALS: Setup Wizard`**。
3.  根据提示，依次选择您的 **英文输入法** 和 **中文输入法**。
4.  配置将自动保存，即刻生效。

### 手动配置 (Advanced)

您也可以在 `settings.json` 中手动设置输入法的 HKL (Hex ID)：

```json
{
    "autoLanguageSwitch.englishLayout": "04090409", // English (US)
    "autoLanguageSwitch.chineseLayout": "08040804"  // Chinese (Simplified)
}
```

## 📂 项目结构 (Structure)

| 目录 | 说明 |
| :--- | :--- |
| **`vscode-extension/`** | 插件前端 (TypeScript)。负责上下文检测和配置管理。 |
| **`native-sidecar/`** | 核心后端 (Rust)。负责系统级输入法枚举与切换。 |

## 🛠️ 开发指南

1.  **编译 Rust Sidecar**:
    ```bash
    cd native-sidecar && cargo build
    ```
2.  **编译 VS Code 插件**:
    ```bash
    cd vscode-extension && npm install && npm run compile
    ```
3.  **调试**:
    *   在 VS Code 中按 **F5** 启动调试窗口。

## 📝 许可证
MIT
